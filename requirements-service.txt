# Зависимости HTTP-сервиса поверх ядра. Ставятся ДОПОЛНИТЕЛЬНО к requirements-infer.txt
# в том же образе: torch, timm, Pillow и NumPy берутся оттуда и здесь не повторяются.
# Версии точные (ответ 39), транзитивные пакеты перечислены явно, pip check проверяет
# согласованность на этапе сборки.
annotated-types==0.8.0
anyio==4.4.0
fastapi==0.115.12
h11==0.16.0
python-multipart==0.0.20
# Совместимы с typing-extensions 4.11.0 из закреплённого базового образа.
# Более новые Pydantic 2.13 / typing-inspection требуют >=4.15, но pip в этой
# conda-базе фактически оставляет 4.12.1 даже при явном пине 4.15.0.
pydantic==2.8.2
pydantic-core==2.20.1
sniffio==1.3.1
starlette==0.46.2
uvicorn==0.34.2
# Векторное хранилище: PostgreSQL с pgvector поднимает docker compose (ТЗ §6).
psycopg==3.2.12
psycopg-binary==3.2.12
# Приближённый поиск для больших галерей (ТЗ §10).
faiss-cpu==1.9.0.post1
