# Закреплённый транзитивный набор (ответ 39: точные версии). Ставится с --no-deps,
# согласованность проверяет pip check в том же слое: образ либо согласован, либо не собирается.
#
# Про цепочку httpx. В рантайме мы в сеть не ходим (HF_HUB_OFFLINE=1, pretrained=False,
# проверено прогоном с --network none), но huggingface_hub 1.32.0 требует httpx при импорте,
# а timm тянет huggingface_hub. Без httpx pip check падает с другой стороны:
#   huggingface-hub 1.32.0 requires httpx, which is not installed.
# Поэтому цепочка возвращена, но anyio взят 4.4.0, а не 4.15.1: 4.15.1 требует
# typing_extensions>=4.16.0, и именно на этом pip check ломался дважды — а пин 4.16.0
# не помогал: pip скачивал 4.16.0 и отчитывался об установке 4.12.1 (очереди 9 и 12).
# anyio 4.4.0 хватает typing_extensions>=4.1, то есть того, что уже есть в базовом образе.
# typing-extensions не пинуется: он из базы, а база зафиксирована по sha256.
# Фактические версии в образе — /app/installed-packages.txt и scripts/image_report.py.
MarkupSafe==2.1.3
PyYAML==6.0.1
anyio==4.4.0
certifi==2024.2.2
click==8.5.0
exceptiongroup==1.2.0
filelock==3.13.1
fsspec==2024.6.0
h11==0.16.0
hf-xet==1.6.0
httpcore==1.0.9
httpx==0.28.1
huggingface-hub==1.32.0
idna==3.7
jinja2==3.1.4
mpmath==1.3.0
networkx==3.2.1
numpy==1.26.4
packaging==23.2
pillow==10.4.0
safetensors==0.8.0
sniffio==1.3.1
sympy==1.12.1
timm==1.0.20
tomli==2.0.1
tqdm==4.66.5
