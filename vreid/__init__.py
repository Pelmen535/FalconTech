"""vreid — ML-ядро сервиса безномерной идентификации ТС.

Модули:
  datasets  — описание сплитов (query/gallery/train) и загрузчики форматов
  models    — backbone'ы для извлечения эмбеддингов
  extract   — прогон датасета через backbone, сохранение эмбеддингов
  index     — FAISS-индекс галереи: поиск, подсчёт соседей в радиусе
  metrics   — стандартный протокол re-ID: CMC (rank-k) и mAP
  bits      — информационная ёмкость наблюдения в битах
"""

__version__ = "0.1.0"
