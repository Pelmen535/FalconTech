# ТОЛЬКО для локальной dev-сборки на базе CUDA 12.8 (карта разработчика — RTX 5060 Ti, sm_120,
# которого нет в релизной базе CUDA 12.1). В сдачу идёт requirements-infer.txt.
#
# Здесь ставится ровно то, чего в новой базе нет: timm и его обязательная цепочка. Всё
# остальное (numpy, pillow, PyYAML, filelock, fsspec, packaging, requests, certifi, idna,
# jinja2, sympy, networkx) берётся из базового образа: перепинить его под релизные версии
# нельзя — torch 2.11 требует более новые (sympy>=1.13.3, packaging>=24.0), и pip check
# это ловит. huggingface_hub взят из ветки 0.34 (клиент на requests, без цепочки httpx и
# без click>=8.4.2): в этой базе click 8.3.1 закреплён пакетом spin, и ветка 1.32 с ним
# не сходится. На инференс версия хаба не влияет — сеть не используется вовсе
# (HF_HUB_OFFLINE=1, pretrained=False), хаб нужен только чтобы timm импортировался.
#
# Следствие, которое надо называть вслух: dev-образ НЕ повторяет релизный набор версий.
# Он доказывает только то, что код детерминирован и работает офлайн в контейнере на этой GPU.
# Переносить его цифры на релизный образ нельзя — релизный проверяется отдельно.
hf-xet==1.6.0
huggingface-hub==0.34.4
safetensors==0.8.0
timm==1.0.20
tqdm==4.66.5
