# Синтетические входные CSV

Это НЕ данные организаторов. Идентификаторы и рамки сгенерированы
`scripts/make_test_fixtures.py` (seed 20260922) и нужны только чтобы проверять ФОРМУ
файлов сдачи: покрытие всех запросов, порядок строк `embeddings.npy`, ровно десять
кандидатов на запрос, формат `candidates.csv`.

Настоящие `test_query.csv` и `test_gallery.csv` выдают организаторы вместе с кадрами;
их разметка в этот репозиторий не выкладывается.
